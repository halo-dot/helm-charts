{{- if .Values.adaptor.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "adaptor.fullname" . }}
  labels:
    {{- include "adaptor.labels" . | nindent 4 }}
  {{- with .Values.adaptor.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.adaptor.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.adaptor.ingress.tls }}
  tls:
    {{- range .Values.adaptor.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "adaptor.fullname" $ }}
                port:
                  number: {{ $.Values.adaptor.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
