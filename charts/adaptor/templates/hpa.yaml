{{- if .Values.adaptor.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "adaptor.fullname" . }}
  labels:
    {{- include "adaptor.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "adaptor.fullname" . }}
  minReplicas: {{ .Values.adaptor.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.adaptor.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.adaptor.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adaptor.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
