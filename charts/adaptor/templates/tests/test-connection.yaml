apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adaptor.fullname" . }}-test-connection"
  labels:
    {{- include "adaptor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adaptor.fullname" . }}:{{ .Values.adaptor.service.port }}']
  restartPolicy: Never
